import path from 'path';
// 中文注释：OCR 字体文件路径
export const OCR_FONT_PATH_ = path.join(process.cwd(), '/src/lib/font/1_cn.txt');

// 截图保存路径
export const SCREENSHOT_PATH = path.join(process.cwd(), '/public/screenshot');
