import { Damo } from '../../auto-plugin/Damo/damo';

// 检查背包装备栏是否装满
export const checkGearTabInBackpackFull = (damo: Damo) => {
  // const isFull = ffoClient.FFO_PluginCall('CheckGearTabInBackpackFull');
  // return isFull === 1;
};

// 检查状态，并确认状态时间
